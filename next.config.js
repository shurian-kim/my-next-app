/** @type {import('next').NextConfig} */

const nextConfig = {
  reactStrictMode: true,
  swcMinify: true,
  // "distDir": "build",
  // "exportTrailingSlash": true,
  // "assetPrefix": "https://~~~"
}

module.exports = nextConfig
