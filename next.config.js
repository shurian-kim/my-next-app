/** @type {import('next').NextConfig} */

const nextConfig = {
  reactStrictMode: true,
  swcMinify: true,
  trailingSlash: true,
  // "distDir": "build",
  // "exportTrailingSlash": true,
  // "assetPrefix": "https://~~~"
}

module.exports = nextConfig;
